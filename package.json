{
  "name": "ai-insights",
  "version": "0.1.0",
  "description": "AI Insights — local web dashboard for AI coding tool usage: token counts, costs, session history, and harness configuration. Currently parses Claude Code's JSONL transcripts; designed to be extended to other providers (Gemini, ChatGPT, local Ollama, …). No external services.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch run server.ts",
    "build": "bun build --compile --target=bun-windows-x64 server.ts --outfile dist/ai-insights.exe",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "chokidar": "^4.0.0",
    "js-tiktoken": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
